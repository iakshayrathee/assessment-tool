generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                          String                       @id @default(cuid())
  email                       String                       @unique
  password                    String
  role                        UserRole
  isActive                    Boolean                      @default(true)
  createdAt                   DateTime                     @default(now())
  updatedAt                   DateTime                     @updatedAt
  lastLogin                   DateTime?
  adminProfile                AdminProfile?
  auditLogs                   AuditLog[]
  centerProfile               CenterProfile?
  parentProfile               ParentProfile?
  schoolViewerProfile         SchoolViewerProfile?
  specialEducatorProfile      SpecialEducatorProfile?
  superSpecialEducatorProfile SuperSpecialEducatorProfile?
  notifications               Notification[]
  requestedApprovals          ApprovalRequest[]            @relation("RequestedApprovals")
  targetUserApprovals         ApprovalRequest[]            @relation("TargetUserApprovals")
  approvedRequests            ApprovalRequest[]            @relation("ApprovedRequests")
  rejectedRequests            ApprovalRequest[]            @relation("RejectedRequests")
  passwordResetTokens         PasswordResetToken[]

  @@map("users")
}

model PasswordResetToken {
  id        String   @id @default(cuid())
  token     String   @unique
  userId    String
  expiresAt DateTime
  used      Boolean  @default(false)
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("password_reset_tokens")
}

model AdminProfile {
  id        String   @id @default(cuid())
  userId    String   @unique
  fullName  String
  phone     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("admin_profiles")
}

model SuperSpecialEducatorProfile {
  id                       String             @id @default(cuid())
  userId                   String             @unique
  fullName                 String
  phone                    String?
  dateOfBirth              DateTime?
  gender                   Gender?
  address                  String?
  primaryLanguage          String?
  secondaryLanguages       String[]
  highestQualification     String?
  fieldOfStudy             String?
  institutionName          String?
  yearOfGraduation         Int?
  rciCertified             Boolean            @default(false)
  rciValidityDate          DateTime?
  specialEdQualification   String?
  specializationAreas      String[]
  yearsOfExperience        Int?
  experienceTypes          String[]
  maxGroupSize             Int?
  currentWorkLocations     String[]
  ldTypesHandled           String[]
  gradeLevelsServed        String[]
  assessmentTools          String?
  assistiveTechProficiency String[]
  areasOfInterest          String[]
  consentToShare           Boolean            @default(false)
  agreementToPolicies      Boolean            @default(false)
  personalStatement        String?
  createdAt                DateTime           @default(now())
  updatedAt                DateTime           @updatedAt
  assignedCenters          CenterAssignment[]
  assignedSchools          SchoolAssignment[]
  reviewedReports          Report[]
  trainingLogs             TrainingLog[]
  user                     User               @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("super_special_educator_profiles")
}

model SpecialEducatorProfile {
  id                       String                   @id @default(cuid())
  userId                   String                   @unique
  fullName                 String
  phone                    String?
  dateOfBirth              DateTime?
  gender                   Gender?
  address                  String?
  primaryLanguage          String?
  secondaryLanguages       String[]
  highestQualification     String?
  fieldOfStudy             String?
  institutionName          String?
  yearOfGraduation         Int?
  rciCertified             Boolean                  @default(false)
  rciValidityDate          DateTime?
  specialEdQualification   String?
  specializationAreas      String[]
  yearsOfExperience        Int?
  experienceTypes          String[]
  maxGroupSize             Int?
  currentWorkLocations     String[]
  ldTypesHandled           String[]
  gradeLevelsServed        String[]
  assessmentTools          String?
  assistiveTechProficiency String[]
  areasOfInterest          String[]
  consentToShare           Boolean                  @default(false)
  agreementToPolicies      Boolean                  @default(false)
  personalStatement        String?
  createdAt                DateTime                 @default(now())
  updatedAt                DateTime                 @updatedAt
  additionalCertifications String[]
  totalYearsOfExperience   Int?
  assessments              Assessment[]
  formalAssessments        FormalAssessment[]
  readingSkillAssessments  ReadingSkillAssessment[]
  writingSkillAssessments  WritingSkillAssessment[]
  mathSkillAssessments     MathSkillAssessment[]
  longTermPlans            LongTermPlan[]
  shortTermPlans           ShortTermPlan[]
  weeklyLessonPlans        WeeklyLessonPlan[]
  homework                 Homework[]
  centerAssignments        CenterAssignment[]
  schoolAssignments        SchoolAssignment[]
  iepGoals                 IEPGoal[]
  intakeForms              IntakeForm[]
  reports                  Report[]
  sessionNotes             SessionNote[]
  trainingLogs             TrainingLog[]
  user                     User                     @relation(fields: [userId], references: [id], onDelete: Cascade)
  assignedStudents         StudentAssignment[]
  ieps                     IEP[]
  iepDocuments             IEPDocument[]

  @@map("special_educator_profiles")
}

model CenterProfile {
  id              String                 @id @default(cuid())
  userId          String                 @unique
  centerName      String
  address         String?
  phone           String?
  email           String?
  contactPerson   String?
  operatingHours  String?
  description     String?
  createdAt       DateTime               @default(now())
  updatedAt       DateTime               @updatedAt
  assignments     CenterAssignment[]
  user            User                   @relation(fields: [userId], references: [id], onDelete: Cascade)
  schools         School[]
  students        Student[]
  reportSnapshots CenterReportSnapshot[]

  @@map("center_profiles")
}

model ParentProfile {
  id               String                 @id @default(cuid())
  userId           String                 @unique
  fullName         String
  phone            String?
  address          String?
  emergencyContact String?
  relationship     String                 @default("Parent")
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  concerns         ParentConcern[]
  documents        ParentDocument[]
  homework         Homework[]
  user             User                   @relation(fields: [userId], references: [id], onDelete: Cascade)
  children         Student[]
  reportSnapshots  ParentReportSnapshot[]

  @@map("parent_profiles")
}

model SchoolViewerProfile {
  id        String   @id @default(cuid())
  userId    String   @unique
  fullName  String
  position  String?
  phone     String?
  schoolId  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  school    School   @relation(fields: [schoolId], references: [id])
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("school_viewer_profiles")
}

model School {
  id              String                 @id @default(cuid())
  name            String
  address         String?
  phone           String?
  email           String?
  principalName   String?
  centerId        String?
  createdAt       DateTime               @default(now())
  updatedAt       DateTime               @updatedAt
  viewers         SchoolViewerProfile[]
  center          CenterProfile?         @relation(fields: [centerId], references: [id])
  students        Student[]
  assignments     SchoolAssignment[]
  reportSnapshots SchoolReportSnapshot[]

  @@map("schools")
}

model Student {
  id                      String                   @id @default(cuid())
  fullName                String
  dateOfBirth             DateTime
  age                     Int
  gender                  Gender
  grade                   String
  motherTongue            String?
  syllabus                String?
  status                  StudentStatus            @default(ACTIVE)
  registrationDate        DateTime                 @default(now())
  createdAt               DateTime                 @default(now())
  updatedAt               DateTime                 @updatedAt
  centerId                String
  schoolId                String?
  parentId                String?
  riskCategory            RiskCategory?            @default(ON_TRACK)
  lastRiskAssessment      DateTime?
  assessments             Assessment[]
  formalAssessments       FormalAssessment[]
  readingSkillAssessments ReadingSkillAssessment[]
  writingSkillAssessments WritingSkillAssessment[]
  mathSkillAssessments    MathSkillAssessment[]
  longTermPlans           LongTermPlan[]
  shortTermPlans          ShortTermPlan[]
  weeklyLessonPlans       WeeklyLessonPlan[]
  homework                Homework[]
  iepGoals                IEPGoal[]
  intakeForms             IntakeForm[]
  reports                 Report[]
  sessionNotes            SessionNote[]
  assignments             StudentAssignment[]
  documents               StudentDocument[]
  center                  CenterProfile            @relation(fields: [centerId], references: [id])
  parent                  ParentProfile?           @relation(fields: [parentId], references: [id])
  school                  School?                  @relation(fields: [schoolId], references: [id])
  ieps                    IEP[]
  iepDocuments            IEPDocument[]
  reportSnapshots         ParentReportSnapshot[]

  @@map("students")
}

model StudentAssignment {
  id                String                 @id @default(cuid())
  studentId         String
  specialEducatorId String
  assignedDate      DateTime               @default(now())
  isActive          Boolean                @default(true)
  createdAt         DateTime               @default(now())
  updatedAt         DateTime               @updatedAt
  specialEducator   SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student           Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@unique([studentId, specialEducatorId])
  @@map("student_assignments")
}

model IEP {
  id                 String    @id @default(cuid())
  studentId          String
  educatorId         String
  startDate          DateTime
  endDate            DateTime
  presentLevel       Json?
  longTermGoals      Json[]
  shortTermGoals     Json[]
  areasOfRemediation String[]
  status             IEPStatus @default(DRAFT)
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt

  student     Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)
  educator    SpecialEducatorProfile @relation(fields: [educatorId], references: [id])
  weeklyPlans WeeklyPlan[]

  @@map("ieps")
}

model WeeklyPlan {
  id            String   @id @default(cuid())
  iepId         String
  weekStartDate DateTime
  weekEndDate   DateTime
  monthlyPlan   Json[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  iep         IEP                @relation(fields: [iepId], references: [id], onDelete: Cascade)
  evaluations WeeklyEvaluation[]

  @@map("weekly_plans")
}

model WeeklyEvaluation {
  id                String   @id @default(cuid())
  weeklyPlanId      String
  evaluationDate    DateTime
  oralLanguage      Json?
  reading           Json?
  writing           Json?
  spelling          Json?
  math              Json?
  attention         String?
  behavioralSitting String?
  taskCompletion    String?
  strategies        String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  weeklyPlan WeeklyPlan @relation(fields: [weeklyPlanId], references: [id], onDelete: Cascade)

  @@map("weekly_evaluations")
}

enum IEPStatus {
  DRAFT
  ACTIVE
  COMPLETED
  ARCHIVED
}

model CenterAssignment {
  id                     String                       @id @default(cuid())
  centerId               String
  specialEducatorId      String?
  superSpecialEducatorId String?
  assignedDate           DateTime                     @default(now())
  isActive               Boolean                      @default(true)
  createdAt              DateTime                     @default(now())
  updatedAt              DateTime                     @updatedAt
  center                 CenterProfile                @relation(fields: [centerId], references: [id])
  specialEducator        SpecialEducatorProfile?      @relation(fields: [specialEducatorId], references: [id])
  superSpecialEducator   SuperSpecialEducatorProfile? @relation(fields: [superSpecialEducatorId], references: [id])

  @@map("center_assignments")
}

model SchoolAssignment {
  id                     String                       @id @default(cuid())
  schoolId               String
  specialEducatorId      String?
  superSpecialEducatorId String?
  assignedDate           DateTime                     @default(now())
  isActive               Boolean                      @default(true)
  createdAt              DateTime                     @default(now())
  updatedAt              DateTime                     @updatedAt
  school                 School                       @relation(fields: [schoolId], references: [id])
  specialEducator        SpecialEducatorProfile?      @relation(fields: [specialEducatorId], references: [id])
  superSpecialEducator   SuperSpecialEducatorProfile? @relation(fields: [superSpecialEducatorId], references: [id])

  @@map("school_assignments")
}

model IntakeForm {
  id                                String                 @id @default(cuid())
  studentId                         String
  specialEducatorId                 String
  address                           String?
  familyIncome                      String?
  familyType                        String?
  digitalResourcesAtHome            Boolean?
  dailyDigitalUse                   Int?
  enjoysSchool                      Boolean?
  studyAssistant                    String?
  externalAcademicSupport           Boolean?
  enjoysReading                     Boolean?
  dailyParentChildTime              Int?
  childType                         String?
  fatherName                        String?
  motherName                        String?
  guardianName                      String?
  pregnancyNormal                   Boolean?
  medicationsDuringPregnancy        String?
  miscarriagesAbortions             Boolean?
  fullTermOrPremature               String?
  deliveryType                      String?
  breastFed                         Boolean?
  infantJaundice                    Boolean?
  incubation                        Boolean?
  immunizationDone                  Boolean?
  consanguineousMarriage            Boolean?
  birthCry                          String?
  delayInNeckStanding               Boolean?
  delayInNeckStandingDetails        String?
  ageOfWalking                      Int?
  ageOfTwoWordSpeech                Int?
  healthConcerns                    String?
  epilepticHistory                  Boolean?
  onMedication                      Boolean?
  medicationDetails                 String?
  asthmaWheezing                    Boolean?
  wearsGlasses                      Boolean?
  visionTestDone                    Boolean?
  hearingTestDone                   Boolean?
  attendedPreschool                 Boolean?
  repeatedGrades                    Boolean?
  whichGradeRepeated                String?
  dominantWritingHand               String?
  strugglesInLanguages              Boolean?
  status                            AssessmentStatus       @default(PENDING)
  completedAt                       DateTime?
  createdAt                         DateTime               @default(now())
  updatedAt                         DateTime               @updatedAt
  medicationsDuringPregnancyDetails String?
  specialEducator                   SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student                           Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("intake_forms")
}

// Informal Assessment (existing structure - now explicitly informal)
model Assessment {
  id                    String                 @id @default(cuid())
  studentId             String
  specialEducatorId     String
  readingObservations   String?
  readingLevel          String?
  readingFiles          String[]
  writingObservations   String?
  writingLevel          String?
  writingFiles          String[]
  mathObservations      String?
  mathLevel             String?
  mathFiles             String[]
  vpObservations        String?
  vpLevel               String?
  vpFiles               String[]
  motorObservations     String?
  motorLevel            String?
  motorFiles            String[]
  attentionObservations String?
  attentionLevel        String?
  attentionFiles        String[]
  assessmentType        String                 @default("Initial")
  status                AssessmentStatus       @default(PENDING)
  completedAt           DateTime?
  createdAt             DateTime               @default(now())
  updatedAt             DateTime               @updatedAt
  specialEducator       SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student               Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("assessments")
}

// Formal Assessment
model FormalAssessment {
  id                String                 @id @default(cuid())
  studentId         String
  specialEducatorId String
  assessmentType    String // Type of formal assessment
  referralReason    String?
  referralDate      DateTime
  referredBy        String // Auto-filled from educator
  conductedBy       String?
  credentials       String?
  clinicName        String?
  assessmentDate    DateTime?
  keyFindings       String?
  diagnosis         String?
  recommendations   String?
  uploadedFiles     String[] // Array of file paths/URLs
  status            AssessmentStatus       @default(PENDING)
  completedAt       DateTime?
  createdAt         DateTime               @default(now())
  updatedAt         DateTime               @updatedAt
  specialEducator   SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student           Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("formal_assessments")
}

// Reading Skill Assessment
model ReadingSkillAssessment {
  id                                  String  @id @default(cuid())
  studentId                           String
  specialEducatorId                   String
  // Assessment Questions
  readingQ1                           String? // Is the child reading at grade level?
  readingQ2                           String? // Can the child decode unfamiliar words?
  readingQ3                           String? // Can the child answer comprehension questions?
  // Decoding & Word Reading Errors
  missesLetters                       Boolean @default(false)
  missesWords                         Boolean @default(false)
  missesSentences                     Boolean @default(false)
  substitution                        Boolean @default(false)
  omissionBeginning                   Boolean @default(false)
  omissionEnding                      Boolean @default(false)
  omissionWholeWord                   Boolean @default(false)
  additionWordsOrSyllables            Boolean @default(false)
  guessingWords                       Boolean @default(false)
  mispronunciation                    Boolean @default(false)
  troubleBlendingSyllables            Boolean @default(false)
  difficultyDecodingUnfamiliar        Boolean @default(false)
  poorWordRecognition                 Boolean @default(false)
  troubleRememberingSightWords        Boolean @default(false)
  troubleLearningLetterSound          Boolean @default(false)
  shortLongVowelConfusion             Boolean @default(false)
  poorSyllabication                   Boolean @default(false)
  // Fluency & Reading Flow
  poorFlowWhileReading                Boolean @default(false)
  choppyReading                       Boolean @default(false)
  lotsOfGaps                          Boolean @default(false)
  wordByWordReading                   Boolean @default(false)
  reReadingSameLine                   Boolean @default(false)
  repetitionOfWords                   Boolean @default(false)
  vocalizeDuringSilentReading         Boolean @default(false)
  poorIntonations                     Boolean @default(false)
  poorPhrasing                        Boolean @default(false)
  slowEffortfulReading                Boolean @default(false)
  // Tracking, Eye Movement, Visual Skills
  movesHeadWhileReading               Boolean @default(false)
  losesPlaceWhileReading              Boolean @default(false)
  skipsLines                          Boolean @default(false)
  poorEyeTracking                     Boolean @default(false)
  poorScanningSkills                  Boolean @default(false)
  holdsBookTooClose                   Boolean @default(false)
  difficultyLeftRightEyeMovement      Boolean @default(false)
  difficultyRecognizingSimilarLetters Boolean @default(false)
  // Comprehension Indicators
  readsWithoutUnderstanding           Boolean @default(false)
  forgetsWhatWasRead                  Boolean @default(false)
  difficultyAnsweringQuestions        Boolean @default(false)
  // Attention & Reading Behavior
  notInterestedInReading              Boolean @default(false)
  avoidsReadingAloud                  Boolean @default(false)
  avoidsReadingActivities             Boolean @default(false)
  yawningFrequently                   Boolean @default(false)
  easilyFrustrated                    Boolean @default(false)
  lowConfidence                       Boolean @default(false)
  poorReadingStamina                  Boolean @default(false)
  // Mechanics & Punctuation
  punctuationErrors                   Boolean @default(false)
  doesNotPauseAtFullStop              Boolean @default(false)
  extraPausesAtCommas                 Boolean @default(false)
  incorrectToneForQuestionExclamation Boolean @default(false)

  // NEW: Initial Reading Level Assessment
  independentLevelKnownText     Boolean @default(false)
  independentLevelUnknownText   Boolean @default(false)
  instructionalLevelKnownText   Boolean @default(false)
  instructionalLevelUnknownText Boolean @default(false)
  frustrationLevelKnownText     Boolean @default(false)
  frustrationLevelUnknownText   Boolean @default(false)

  // NEW: Reading Grade Level Mapping
  isAtGradeLevel        Boolean?
  functionalGradeLevel  String? // Editable grade level if at grade level
  performanceSummary    String? // Free text for performance summary
  gradeLevelMappings    Json? // Array of {gradeLevel, independent, instructional, frustration}
  gradeLevelObservation String? // Observation for grade mappings

  // NEW: Knowledcare Battery Test Results
  batteryTestConducted Boolean @default(false)
  batteryTestSummary   String?
  batteryTestReportUrl String? // PDF/Report upload path

  // NEW: Reading Comprehension Level
  atGradeLevelComprehension Boolean?
  comprehensionLevels       String[] @default([]) // Literal, Inferential, Application, Critical
  currentLevelComprehension String[] @default([]) // If not at grade level
  comprehensionObservation  String?

  additionalNotes String?
  status          AssessmentStatus       @default(PENDING)
  completedAt     DateTime?
  createdAt       DateTime               @default(now())
  updatedAt       DateTime               @updatedAt
  specialEducator SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student         Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("reading_skill_assessments")
}

// Writing Skill Assessment
model WritingSkillAssessment {
  id                                 String  @id @default(cuid())
  studentId                          String
  specialEducatorId                  String
  // Assessment Questions
  writingQ1                          String? // Can the child write legibly?
  writingQ2                          String? // Does the child use proper letter formation?
  writingQ3                          String? // Can the child compose sentences?
  // Fine Motor & Grip Issues
  incorrectPencilGrip                Boolean @default(false)
  holdsPencilTooTightly              Boolean @default(false)
  holdsPencilTooLoosely              Boolean @default(false)
  writesExcessivePressure            Boolean @default(false)
  writesLightPressure                Boolean @default(false)
  wristFingerPainComplaints          Boolean @default(false)
  slowFineMotorSpeed                 Boolean @default(false)
  fatigueAfterShortWriting           Boolean @default(false)
  // Letter Formation Issues
  incorrectLetterFormation           Boolean @default(false)
  reversals                          Boolean @default(false)
  difficultiesFormingCurvesDiagonals Boolean @default(false)
  lettersWrittenMirrorImage          Boolean @default(false)
  poorStrokeSequence                 Boolean @default(false)
  capitalsInsertedBetweenWords       Boolean @default(false)
  difficultyCopyingLetters           Boolean @default(false)
  // Spacing, Alignment & Presentation
  poorSpacingBetweenLetters          Boolean @default(false)
  poorSpacingBetweenWords            Boolean @default(false)
  writesOutsideLine                  Boolean @default(false)
  difficultyMaintainingBaseline      Boolean @default(false)
  unevenLetterSize                   Boolean @default(false)
  inconsistentSpacingAcrossPage      Boolean @default(false)
  crowdedWriting                     Boolean @default(false)
  tooMuchSpaceBetweenLetters         Boolean @default(false)
  floatingLettersAboveLine           Boolean @default(false)
  // Handwriting Fluency
  verySlowWriting                    Boolean @default(false)
  writesTooFastManyErrors            Boolean @default(false)
  poorHandwritingEndurance           Boolean @default(false)
  choppyWriting                      Boolean @default(false)
  inconsistentPace                   Boolean @default(false)
  repeatedErasing                    Boolean @default(false)
  frequentCorrections                Boolean @default(false)
  // Dictation & Spelling
  difficultyWritingDictatedLetters   Boolean @default(false)
  difficultyWritingDictatedWords     Boolean @default(false)
  spellsPhonetically                 Boolean @default(false)
  omitsLettersInSpelling             Boolean @default(false)
  addsExtraLetters                   Boolean @default(false)
  substitutesLettersOrSounds         Boolean @default(false)
  confusesVowelSounds                Boolean @default(false)
  troubleEncodingCVC                 Boolean @default(false)
  troubleEncodingBlendsDigraphs      Boolean @default(false)
  // Sentence Formation / Written Expression
  cannotConstructSimpleSentences     Boolean @default(false)
  writesOnlySingleWords              Boolean @default(false)
  strugglesExpandSentences           Boolean @default(false)
  poorGrammarUsage                   Boolean @default(false)
  writesIncompleteSentences          Boolean @default(false)
  confusingSentenceOrder             Boolean @default(false)
  difficultyExpressingIdeas          Boolean @default(false)
  avoidsWrittenTasks                 Boolean @default(false)
  needsVerbalPromptsToWrite          Boolean @default(false)
  // Copying Skills
  difficultyCopyingFromBoard         Boolean @default(false)
  difficultyCopyingFromBook          Boolean @default(false)
  slowCopying                        Boolean @default(false)
  skipsWordsOrLettersWhenCopying     Boolean @default(false)
  copiesInaccurately                 Boolean @default(false)
  looksAwayFrequentlyWhileCopying    Boolean @default(false)
  // Organization & Structure
  writingDisorganized                Boolean @default(false)
  thoughtsNotLogicallySequenced      Boolean @default(false)
  cannotPlanWriting                  Boolean @default(false)
  beginsWritingRandomAreasOnPage     Boolean @default(false)
  noConceptOfMargins                 Boolean @default(false)
  paragraphingDifficulty             Boolean @default(false)
  // Behavioral / Self-Management Issues
  avoidsWritingActivities            Boolean @default(false)
  complainsWritingIsHard             Boolean @default(false)
  getsFrustratedQuickly              Boolean @default(false)
  lowWritingStamina                  Boolean @default(false)
  givesUpInMiddleOfTask              Boolean @default(false)
  lowConfidenceWriting               Boolean @default(false)
  inconsistentPerformanceAcrossDays  Boolean @default(false)

  // NEW: Near Copying Skills
  hasNearCopyingSkills   Boolean?
  nearCopyingLevels      String[] @default([]) // Letter, Word, Phrase, Sentence
  nearCopyingObservation String?

  // NEW: Board Copying Skills
  hasBoardCopyingSkills        Boolean?
  boardCopyingLevels           String[] @default([]) // Letter, Word, Phrase, Sentence
  boardCopyingSpeedObservation String?
  visualTrackingDifficulty     Boolean  @default(false)
  omissionSkippingFlag         Boolean  @default(false)
  boardCopyingObservation      String?

  // NEW: Punctuation Skills (Legacy - kept for backward compatibility)
  usesCapitalLetters     Boolean @default(false)
  usesFullStop           Boolean @default(false)
  usesQuestionMark       Boolean @default(false)
  usesComma              Boolean @default(false)
  usesApostrophe         Boolean @default(false)
  punctuationOther       String?
  punctuationObservation String?

  // NEW: Punctuation Skills (for Copying)
  usesCapitalLettersCopying     Boolean @default(false)
  usesFullStopCopying           Boolean @default(false)
  usesQuestionMarkCopying       Boolean @default(false)
  usesCommaCopying              Boolean @default(false)
  usesApostropheCopying         Boolean @default(false)
  punctuationOtherCopying       String?
  punctuationObservationCopying String?

  // NEW: Punctuation Skills (in Creative Writing)
  usesCapitalLettersCreative     Boolean @default(false)
  usesFullStopCreative           Boolean @default(false)
  usesQuestionMarkCreative       Boolean @default(false)
  usesCommaCreative              Boolean @default(false)
  usesApostropheCreative         Boolean @default(false)
  punctuationOtherCreative       String?
  punctuationObservationCreative String?

  // NEW: Spelling Observations
  spellingStrengthSummary         String?
  spellingErrorPatternObservation String?

  // NEW: Creative Writing
  creativeWritingSummary String?

  additionalNotes String?
  status          AssessmentStatus       @default(PENDING)
  completedAt     DateTime?
  createdAt       DateTime               @default(now())
  updatedAt       DateTime               @updatedAt
  specialEducator SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student         Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("writing_skill_assessments")
}

// Math Skill Assessment
model MathSkillAssessment {
  id                                  String  @id @default(cuid())
  studentId                           String
  specialEducatorId                   String
  // Assessment Questions
  mathQ1                              String? // Does the child understand number concepts?
  mathQ2                              String? // Can the child perform basic operations?
  mathQ3                              String? // Can the child solve word problems?
  // Number Sense & Number Identification
  difficultyIdentifyingNumbers1to10   Boolean @default(false)
  difficultyIdentifyingNumbers1to20   Boolean @default(false)
  difficultyIdentifyingNumbers1to100  Boolean @default(false)
  reversesNumbers                     Boolean @default(false)
  writesNumbersIncorrectly            Boolean @default(false)
  difficultySequencingNumbers         Boolean @default(false)
  skipsNumbersWhileCounting           Boolean @default(false)
  countsSlowlyOrWithEffort            Boolean @default(false)
  troubleWithForwardCounting          Boolean @default(false)
  troubleWithBackwardCounting         Boolean @default(false)
  difficultyWithSkipCounting          Boolean @default(false)
  doesNotUnderstandQuantity           Boolean @default(false)
  cannotMatchNumberToQuantity         Boolean @default(false)
  cannotCompareNumbers                Boolean @default(false)
  difficultyIdentifyingPlaceValue     Boolean @default(false)
  // Basic Operations: Addition & Subtraction
  strugglesSingleDigitAddition        Boolean @default(false)
  strugglesSingleDigitSubtraction     Boolean @default(false)
  cannotCarryOver                     Boolean @default(false)
  cannotBorrow                        Boolean @default(false)
  usesFingerCountingExcessively       Boolean @default(false)
  cannotPerformMentalMath             Boolean @default(false)
  doesNotUnderstandPlusMinusSymbols   Boolean @default(false)
  confusesAdditionSubtraction         Boolean @default(false)
  difficultyWithWordProblems          Boolean @default(false)
  cannotUnderstandRealWorldMath       Boolean @default(false)
  // Concepts & Pre-Math Skills
  difficultyUnderstandingPatterns     Boolean @default(false)
  difficultyFinishingPatterns         Boolean @default(false)
  troubleIdentifyingShapes            Boolean @default(false)
  troubleSortingObjects               Boolean @default(false)
  difficultyInMatching                Boolean @default(false)
  difficultyWithSpatialConcepts       Boolean @default(false)
  difficultyUnderstandingMeasurement  Boolean @default(false)
  difficultyWithTimeConcepts          Boolean @default(false)
  difficultyReadingClock              Boolean @default(false)
  // Math Fluency & Working Speed
  verySlowInSolvingProblems           Boolean @default(false)
  frequentCalculationMistakes         Boolean @default(false)
  poorWorkingMemoryForMath            Boolean @default(false)
  troubleRememberingMathFacts         Boolean @default(false)
  difficultyRememberingSteps          Boolean @default(false)
  needsRepeatedInstructions           Boolean @default(false)
  getsConfusedDuringMultiStep         Boolean @default(false)
  // Visual-Spatial & Alignment Issues
  misalignsNumbersInColumns           Boolean @default(false)
  writesNumbersOutsideGrid            Boolean @default(false)
  poorSpatialOrganization             Boolean @default(false)
  placesDigitsInWrongOrder            Boolean @default(false)
  drawsShapesIncorrectly              Boolean @default(false)
  cannotVisuallyGroupObjects          Boolean @default(false)
  difficultyCopyingMathFromBoard      Boolean @default(false)
  // Symbol & Concept Confusion
  confusesMathSymbols                 Boolean @default(false)
  cannotUnderstandEqualsMeansSameAs   Boolean @default(false)
  treatsEqualsAsAnswerComesAfter      Boolean @default(false)
  difficultyRememberingOperationRules Boolean @default(false)
  cannotDifferentiateTensOnes         Boolean @default(false)
  misunderstandsMoreLess              Boolean @default(false)
  // Behavioral & Learning Indicators
  avoidsMathTasks                     Boolean @default(false)
  lowMathConfidence                   Boolean @default(false)
  givesUpQuickly                      Boolean @default(false)
  anxiousDuringMathActivities         Boolean @default(false)
  needsConstantPrompting              Boolean @default(false)
  appearsConfusedAfterExplanation     Boolean @default(false)
  poorAttentionDuringMath             Boolean @default(false)

  // NEW: Math Grade Level Identification
  isAtMathGradeLevel        Boolean?
  mathFunctionalGradeLevel  String? // Editable grade level
  mathPerformanceSummary    String?
  mathGradeLevelMappings    Json? // Array of {gradeLevel, independent, instructional, frustration, summaryNote}
  mathGradeLevelObservation String?

  // NEW: Knowledcare Battery Test (Math)
  mathBatteryTestConducted Boolean @default(false)
  mathBatteryTestSummary   String?
  mathBatteryTestReportUrl String?

  // NEW: Math Concepts Performance (Json objects with performance, summary, errorPattern)
  additionPerformance       Json?
  subtractionPerformance    Json?
  multiplicationPerformance Json?
  divisionPerformance       Json?
  placeValuePerformance     Json?
  numberLinePerformance     Json?
  fractionsPerformance      Json?
  decimalsPerformance       Json?
  algebraPerformance        Json?
  statementSumsPerformance  Json?
  geometryPerformance       Json?

  additionalNotes String?
  status          AssessmentStatus       @default(PENDING)
  completedAt     DateTime?
  createdAt       DateTime               @default(now())
  updatedAt       DateTime               @updatedAt
  specialEducator SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student         Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("math_skill_assessments")
}

// Long-Term Plan (LTP) - 6-12 months
model LongTermPlan {
  id                String @id @default(cuid())
  studentId         String @map("student_id")
  specialEducatorId String @map("special_educator_id")

  // Student Profile Summary
  diagnosis         String?
  suspectedLD       String?  @map("suspected_ld")
  learningStrengths String[] @map("learning_strengths")
  challengeAreas    String[] @map("challenge_areas")

  // Duration
  startDate      DateTime @map("start_date")
  endDate        DateTime @map("end_date")
  durationMonths Int      @map("duration_months") // 6-12 months

  // Domains Covered
  domains Domain[]

  // Review Cycle
  reviewCycle    ReviewCycle @default(QUARTERLY) @map("review_cycle")
  lastReviewDate DateTime?   @map("last_review_date")
  nextReviewDate DateTime    @map("next_review_date")

  // Status
  status PlanStatus @default(ACTIVE)

  // Timestamps
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Relations
  student         Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)
  specialEducator SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  goals           LongTermGoal[]
  shortTermPlans  ShortTermPlan[]

  @@map("long_term_plans")
}

// Long-Term Goals (3-5 per LTP)
model LongTermGoal {
  id             String @id @default(cuid())
  longTermPlanId String @map("long_term_plan_id")

  goalStatement  String @map("goal_statement") // e.g., "Student will read CVC words with 80% accuracy"
  domain         Domain
  targetAccuracy Int    @default(80) @map("target_accuracy") // percentage

  order        Int // for sorting (1-5)
  isAchieved   Boolean   @default(false) @map("is_achieved")
  achievedDate DateTime? @map("achieved_date")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  longTermPlan LongTermPlan @relation(fields: [longTermPlanId], references: [id], onDelete: Cascade)

  @@map("long_term_goals")
}

// Short-Term Plan (STP) - 4-8 weeks
model ShortTermPlan {
  id                String @id @default(cuid())
  longTermPlanId    String @map("long_term_plan_id")
  studentId         String @map("student_id")
  specialEducatorId String @map("special_educator_id")

  // Link to LTP Goal
  linkedLongTermGoalId String? @map("linked_long_term_goal_id")
  linkedGoalStatement  String  @map("linked_goal_statement") // Copy of LTP goal for reference

  // Duration
  startDate     DateTime @map("start_date")
  endDate       DateTime @map("end_date")
  durationWeeks Int      @map("duration_weeks") // 4-8 weeks

  // STP Goal
  stpGoal String @map("stp_goal") // Main STP goal statement

  // Intervention Strategy
  interventionStrategy String[] @map("intervention_strategy") // ["Multi-sensory", "Tracing", "Flashcards", etc.]

  // Success Metrics
  weeklyProbes    Boolean @default(true) @map("weekly_probes")
  targetAccuracy  Int     @default(80) @map("target_accuracy")
  promptReduction Boolean @default(true) @map("prompt_reduction")

  // Status & Progress
  status             PlanStatus @default(ACTIVE)
  progressPercentage Int        @default(0) @map("progress_percentage")

  // Timestamps
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Relations
  longTermPlan      LongTermPlan           @relation(fields: [longTermPlanId], references: [id], onDelete: Cascade)
  student           Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)
  specialEducator   SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  subGoals          ShortTermSubGoal[]
  weeklyLessonPlans WeeklyLessonPlan[]

  @@map("short_term_plans")
}

// Short-Term Sub-Goals
model ShortTermSubGoal {
  id              String @id @default(cuid())
  shortTermPlanId String @map("short_term_plan_id")

  goalStatement String    @map("goal_statement") // e.g., "Recognise A-F"
  order         Int // for sorting
  isAchieved    Boolean   @default(false) @map("is_achieved")
  achievedDate  DateTime? @map("achieved_date")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  shortTermPlan ShortTermPlan @relation(fields: [shortTermPlanId], references: [id], onDelete: Cascade)

  @@map("short_term_sub_goals")
}

// Weekly Lesson Plan (WLP) - 1 week
model WeeklyLessonPlan {
  id                String  @id @default(cuid())
  shortTermPlanId   String? @map("short_term_plan_id") // Optional link to STP
  studentId         String  @map("student_id")
  specialEducatorId String  @map("special_educator_id")

  // Week Information
  weekNumber  Int      @map("week_number")
  sessionDate DateTime @map("session_date") // Specific date of session (e.g., 11-11-2025)

  // Topics & Areas
  topics             String // e.g., "Letter Recognition A-C"
  areasOfRemediation String[] @map("areas_of_remediation") // e.g., ["Visual Discrimination", "Auditory Processing"]

  // Time Management
  averageTime Int? @map("average_time") // in minutes (planned)
  actualTime  Int? @map("actual_time") // in minutes (filled after session)

  // Motivation & Resources
  motivationStrategy String?  @map("motivation_strategy") // e.g., "Token reward, praise"
  resourcesUsed      String[] @map("resources_used") // e.g., ["Flashcards", "Tracing sheets", "Digital game"]

  // Outcome
  outcome String? // e.g., "Identified A & B, needed help with C"

  // Status
  status LessonStatus @default(PLANNED)

  // Timestamps
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Relations
  shortTermPlan   ShortTermPlan?         @relation(fields: [shortTermPlanId], references: [id], onDelete: SetNull)
  student         Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)
  specialEducator SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])

  @@map("weekly_lesson_plans")
}

// Homework Assignment
model Homework {
  id                String                 @id @default(cuid())
  studentId         String
  specialEducatorId String
  parentId          String?
  subject           SkillArea // Reading / Writing / Math
  title             String
  instructions      String
  attachedFiles     String[] // PDF / Image / Link
  dueDate           DateTime
  additionalNotes   String?
  estimatedTime     Int? // in minutes
  skillTargeted     String? // Mapped from lesson plan
  status            HomeworkStatus         @default(ASSIGNED)
  submittedAt       DateTime?
  parentFeedback    String?
  educatorFeedback  String?
  createdAt         DateTime               @default(now())
  updatedAt         DateTime               @updatedAt
  specialEducator   SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student           Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)
  parent            ParentProfile?         @relation(fields: [parentId], references: [id])

  @@map("homework")
}

// Learning Materials
model LearningMaterial {
  id          String    @id @default(cuid())
  title       String
  subject     SkillArea // Reading / Writing / Math
  grade       Int // 1-6
  category    String? // Worksheets, Videos, Activities, etc.
  description String?
  fileUrl     String? // URL or path to the material
  fileType    String? // PDF, Video, Image, etc.
  tags        String[] // For better searchability
  isPublic    Boolean   @default(true)
  uploadedBy  String? // Educator ID
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@map("learning_materials")
}

model IEPDocument {
  id                 String                 @id @default(cuid())
  studentId          String
  specialEducatorId  String
  title              String
  durationMonths     Int
  startDate          DateTime
  endDate            DateTime
  areasOfRemediation String[]               @default([])
  status             IEPStatus              @default(DRAFT)
  createdAt          DateTime               @default(now())
  updatedAt          DateTime               @updatedAt
  specialEducator    SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student            Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)
  subjectSections    IEPSubjectSection[]
  weeklyEvaluations  IEPWeeklyEvaluation[]

  @@map("iep_documents")
}

model IEPSubjectSection {
  id                     String             @id @default(cuid())
  iepDocumentId          String
  subject                IEPSubject
  presentLevelReceptive  String?
  presentLevelExpressive String?
  longTermGoals          IEPLongTermGoal[]
  shortTermGoals         IEPShortTermGoal[]
  createdAt              DateTime           @default(now())
  updatedAt              DateTime           @updatedAt
  iepDocument            IEPDocument        @relation(fields: [iepDocumentId], references: [id], onDelete: Cascade)

  @@map("iep_subject_sections")
}

model IEPLongTermGoal {
  id               String            @id @default(cuid())
  subjectSectionId String
  goalNumber       Int
  description      String
  durationMonths   Int
  createdAt        DateTime          @default(now())
  updatedAt        DateTime          @updatedAt
  subjectSection   IEPSubjectSection @relation(fields: [subjectSectionId], references: [id], onDelete: Cascade)

  @@map("iep_long_term_goals")
}

model IEPShortTermGoal {
  id                String            @id @default(cuid())
  subjectSectionId  String
  goalNumber        Int
  description       String
  teacherAssistance String?
  targetDate        DateTime
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  subjectSection    IEPSubjectSection @relation(fields: [subjectSectionId], references: [id], onDelete: Cascade)

  @@map("iep_short_term_goals")
}

model IEPWeeklyEvaluation {
  id            String              @id @default(cuid())
  iepDocumentId String
  weekNumber    Int
  startDate     DateTime
  endDate       DateTime
  activities    IEPWeeklyActivity[]
  strategies    String?
  observations  String?
  createdAt     DateTime            @default(now())
  updatedAt     DateTime            @updatedAt
  iepDocument   IEPDocument         @relation(fields: [iepDocumentId], references: [id], onDelete: Cascade)

  @@map("iep_weekly_evaluations")
}

model IEPWeeklyActivity {
  id                 String                      @id @default(cuid())
  weeklyEvaluationId String
  day                DayOfWeek?
  subject            IEPSubject
  activity           String
  analysis           String?
  assessment         String?
  attentionLevel     BehavioralAttentionLevel?
  sittingTolerance   BehavioralSittingTolerance?
  taskCompletion     BehavioralTaskCompletion?
  createdAt          DateTime                    @default(now())
  updatedAt          DateTime                    @updatedAt
  weeklyEvaluation   IEPWeeklyEvaluation         @relation(fields: [weeklyEvaluationId], references: [id], onDelete: Cascade)

  @@map("iep_weekly_activities")
}

model IEPGoal {
  id                String                 @id @default(cuid())
  studentId         String
  specialEducatorId String
  domain            String
  goalStatement     String
  strategy          String?
  startDate         DateTime
  targetDate        DateTime
  expectedOutcome   String?
  progressPercent   Int                    @default(0)
  status            IEPGoalStatus          @default(NOT_STARTED)
  notes             String?
  createdAt         DateTime               @default(now())
  updatedAt         DateTime               @updatedAt
  specialEducator   SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student           Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)
  progressUpdates   IEPProgress[]

  @@map("iep_goals")
}

model IEPProgress {
  id         String   @id @default(cuid())
  goalId     String
  updateDate DateTime @default(now())
  progress   Int
  notes      String?
  rating     String?
  createdAt  DateTime @default(now())
  goal       IEPGoal  @relation(fields: [goalId], references: [id], onDelete: Cascade)

  @@map("iep_progress")
}

model SessionNote {
  id                String                 @id @default(cuid())
  studentId         String
  specialEducatorId String
  sessionDate       DateTime
  duration          Int?
  activities        String
  observations      String?
  progress          String?
  nextSteps         String?
  createdAt         DateTime               @default(now())
  updatedAt         DateTime               @updatedAt
  specialEducator   SpecialEducatorProfile @relation(fields: [specialEducatorId], references: [id])
  student           Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("session_notes")
}

model Report {
  id                     String                       @id @default(cuid())
  studentId              String
  specialEducatorId      String
  superSpecialEducatorId String?
  type                   ReportType
  title                  String
  content                String
  summary                String?
  recommendations        String?
  educatorSignature      String?
  status                 AssessmentStatus             @default(PENDING)
  submittedAt            DateTime?
  reviewedAt             DateTime?
  createdAt              DateTime                     @default(now())
  updatedAt              DateTime                     @updatedAt
  specialEducator        SpecialEducatorProfile       @relation(fields: [specialEducatorId], references: [id])
  student                Student                      @relation(fields: [studentId], references: [id], onDelete: Cascade)
  superSpecialEducator   SuperSpecialEducatorProfile? @relation(fields: [superSpecialEducatorId], references: [id])

  @@map("reports")
}

model ParentConcern {
  id          String        @id @default(cuid())
  parentId    String
  studentId   String?
  title       String
  description String
  category    String?
  priority    String        @default("Medium")
  status      String        @default("Open")
  response    String?
  respondedAt DateTime?
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt
  parent      ParentProfile @relation(fields: [parentId], references: [id], onDelete: Cascade)

  @@map("parent_concerns")
}

model StudentDocument {
  id          String   @id @default(cuid())
  studentId   String
  fileName    String
  filePath    String
  fileType    String
  fileSize    Int
  category    String?
  description String?
  uploadedBy  String
  createdAt   DateTime @default(now())
  student     Student  @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@map("student_documents")
}

model ParentDocument {
  id          String        @id @default(cuid())
  parentId    String
  fileName    String
  filePath    String
  fileType    String
  fileSize    Int
  category    String?
  description String?
  createdAt   DateTime      @default(now())
  parent      ParentProfile @relation(fields: [parentId], references: [id], onDelete: Cascade)

  @@map("parent_documents")
}

model AuditLog {
  id         String   @id @default(cuid())
  userId     String
  action     String
  resource   String
  resourceId String?
  details    String?
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime @default(now())
  user       User     @relation(fields: [userId], references: [id])

  @@map("audit_logs")
}

model Notification {
  id        String           @id @default(cuid())
  userId    String
  type      NotificationType
  title     String
  message   String
  isRead    Boolean          @default(false)
  data      String? // JSON data for additional context
  createdAt DateTime         @default(now())
  readAt    DateTime?
  user      User             @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, isRead])
  @@index([userId, createdAt])
  @@map("notifications")
}

model TrainingLog {
  id                     String                      @id @default(cuid())
  superSpecialEducatorId String
  specialEducatorId      String?
  title                  String
  description            String
  type                   TrainingType                @default(MENTORSHIP)
  duration               Int? // Duration in minutes
  participants           String[] // Array of educator IDs
  notes                  String?
  followUpRequired       Boolean                     @default(false)
  followUpDate           DateTime?
  createdAt              DateTime                    @default(now())
  updatedAt              DateTime                    @updatedAt
  superSpecialEducator   SuperSpecialEducatorProfile @relation(fields: [superSpecialEducatorId], references: [id])
  specialEducator        SpecialEducatorProfile?     @relation(fields: [specialEducatorId], references: [id])

  @@map("training_logs")
}

enum UserRole {
  ADMIN
  SUPER_SPECIAL_EDUCATOR
  SPECIAL_EDUCATOR
  CENTER
  PARENT
  SCHOOL_VIEWER
  STUDENT
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum AssessmentStatus {
  PENDING
  IN_PROGRESS
  COMPLETED
  REVIEWED
}

enum IEPGoalStatus {
  NOT_STARTED
  IN_PROGRESS
  ACHIEVED
  DISCONTINUED
}

enum IEPSubject {
  ORAL_LANGUAGE
  READING
  WRITING
  SPELLING
  MATH
}

enum BehavioralAttentionLevel {
  POOR
  FAIR
  GOOD
  EXCELLENT
}

enum BehavioralSittingTolerance {
  POOR
  FAIR
  GOOD
  EXCELLENT
}

enum BehavioralTaskCompletion {
  NOT_COMPLETED
  PARTIALLY_COMPLETED
  COMPLETED_WITH_ASSISTANCE
  COMPLETED_INDEPENDENTLY
}

enum ReportType {
  INTAKE
  ASSESSMENT
  IEP
  PROGRESS
  LESSON_PLAN
  AI_COMPREHENSIVE
}

enum StudentStatus {
  ACTIVE
  INACTIVE
  GRADUATED
  TRANSFERRED
}

enum TrainingType {
  MENTORSHIP
  WORKSHOP
  OBSERVATION
  FEEDBACK_SESSION
  PROFESSIONAL_DEVELOPMENT
  CASE_CONSULTATION
}

enum SkillArea {
  READING
  WRITING
  MATH
}

enum MotivationLevel {
  HIGH
  MEDIUM
  LOW
}

enum HomeworkStatus {
  ASSIGNED
  IN_PROGRESS
  SUBMITTED
  REVIEWED
  COMPLETED
}

enum DayOfWeek {
  MONDAY
  TUESDAY
  WEDNESDAY
  THURSDAY
  FRIDAY
}

enum ApprovalRequestType {
  USER_CREATION
  ROLE_ASSIGNMENT
  CENTER_CREATION
  SCHOOL_CREATION
}

enum ApprovalStatus {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
}

model ApprovalRequest {
  id              String              @id @default(cuid())
  type            ApprovalRequestType
  status          ApprovalStatus      @default(PENDING)
  requestedById   String
  targetUserId    String? // For user creation/role assignment
  targetCenterId  String? // For center creation
  targetSchoolId  String? // For school creation
  requestedRole   UserRole? // For role assignment requests
  requestedData   Json // Store the full request data
  comments        String?
  rejectionReason String?
  approvedById    String?
  rejectedById    String?
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt
  approvedAt      DateTime?
  rejectedAt      DateTime?

  // Relations
  requestedBy User  @relation("RequestedApprovals", fields: [requestedById], references: [id])
  targetUser  User? @relation("TargetUserApprovals", fields: [targetUserId], references: [id])
  approvedBy  User? @relation("ApprovedRequests", fields: [approvedById], references: [id])
  rejectedBy  User? @relation("RejectedRequests", fields: [rejectedById], references: [id])

  @@map("approval_requests")
}

// Lesson Plan Enums
enum Domain {
  READING
  WRITING
  MATH
  COGNITIVE
  MOTOR
  BEHAVIOURAL
  READING_COMPREHENSION
  ORAL_LANGUAGE
  SPELLING
}

enum ReviewCycle {
  MONTHLY
  QUARTERLY
  BIANNUAL
}

enum PlanStatus {
  DRAFT
  ACTIVE
  COMPLETED
  ARCHIVED
  ON_HOLD
}

enum LessonStatus {
  PLANNED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum NotificationType {
  // Student Management
  STUDENT_ASSIGNED
  STUDENT_UNASSIGNED

  // Assessments
  ASSESSMENT_CREATED
  ASSESSMENT_COMPLETED
  ASSESSMENT_REVIEWED

  // IEP & Goals
  IEP_GOAL_CREATED
  IEP_GOAL_UPDATED
  IEP_GOAL_ACHIEVED

  // Homework
  HOMEWORK_ASSIGNED
  HOMEWORK_SUBMITTED
  HOMEWORK_REVIEWED

  // Reports
  REPORT_SUBMITTED
  REPORT_APPROVED
  REPORT_REJECTED

  // Parent Concerns
  CONCERN_SUBMITTED
  CONCERN_RESPONDED

  // Lesson Plans
  LESSON_PLAN_CREATED
  LESSON_PLAN_UPDATED

  // Documents
  DOCUMENT_UPLOADED

  // System
  ACCOUNT_CREATED
  ROLE_CHANGED
}

enum RiskCategory {
  HIGH_SUPPORT
  MODERATE_SUPPORT
  ON_TRACK
}

enum ReportPeriodType {
  MONTHLY
  QUARTERLY
  YEARLY
}

model SchoolReportSnapshot {
  id          String           @id @default(cuid())
  schoolId    String
  periodType  ReportPeriodType
  periodStart DateTime
  periodEnd   DateTime

  // Reach & Coverage
  totalEnrolled  Int
  totalScreened  Int
  totalSupported Int
  gradesCovered  String[]

  // Risk Categories
  highSupportCount         Int
  moderateSupportCount     Int
  onTrackCount             Int
  highSupportReduction     Float?
  moderateSupportReduction Float?
  onTrackIncrease          Float?

  // Skill Area Metrics (% students needing support)
  readingReadinessPercent    Float?
  writingReadinessPercent    Float?
  numeracyReadinessPercent   Float?
  attentionEngagementPercent Float?
  processingMemoryPercent    Float?

  // Impact Metrics
  totalSessions      Int
  averageImprovement Json? // Per domain improvements

  // New Dashboard 1 Metrics - Student Progress Levels
  newStudentsThisMonth       Int?
  studentsImproving          Int?
  studentsStable             Int?
  studentsRequiringAttention Int?

  // New Dashboard 3 Metrics - Time Saved for Teachers (in hours)
  manualObservationTimeSaved      Int?
  lessonPlanningTimeSaved         Int?
  trackingWorkloadTimeSaved       Int?
  differentiationSupportTimeSaved Int?
  totalTimeSaved                  Int?

  // New Dashboard 3 Metrics - Evidence of Interventions
  individualSupportPlansCreated    Int?
  smallGroupInterventions          Int?
  classroomStrategyRecommendations Int?
  reviewCyclesCompleted            Int?

  // AI-Generated Narratives
  executiveSummary  String?
  coverageNarrative String?
  impactNarrative   String?
  recommendations   String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  school School @relation(fields: [schoolId], references: [id], onDelete: Cascade)

  @@index([schoolId, periodType, periodStart])
  @@map("school_report_snapshots")
}

model CenterReportSnapshot {
  id          String           @id @default(cuid())
  centerId    String
  periodType  ReportPeriodType
  periodStart DateTime
  periodEnd   DateTime

  // Student Coverage Statistics
  totalStudentsRegistered   Int
  studentsAssessed          Int
  studentsUnderIntervention Int
  newStudentsThisPeriod     Int
  activeStudents            Int
  exitedMainstreamed        Int

  // Assessment Statistics
  totalAssessmentsConducted Int
  baselineAssessments       Int
  reviewProgressAssessments Int
  averageAssessmentTime     Float?
  assessmentsPerEducator    Float?

  // Intervention Statistics
  individualInterventionPlans Int
  smallGroupInterventions     Int
  totalInterventionSessions   Int
  avgSessionsPerStudent       Float?
  avgDurationPerSession       Float?

  // Progress & Outcome Statistics (by domain)
  readingImprovement           Float?
  writingImprovement           Float?
  mathematicsImprovement       Float?
  attentionBehaviorImprovement Float?

  // Educator Productivity Metrics
  activeSpecialEducators Int
  avgStudentsPerEducator Float?
  avgSessionsPerEducator Float?
  avgReportsGenerated    Float?

  // Compliance & Documentation Statistics (percentages)
  assessmentRecordsAvailable  Float?
  interventionPlansDocumented Float?
  progressReviewsCompleted    Float?
  parentReportsShared         Float?

  // Coverage Information
  schoolsCovered String[]
  gradesCovered  String[]

  // AI-Generated Narratives
  executiveSummary String?
  recommendations  String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  center CenterProfile @relation(fields: [centerId], references: [id], onDelete: Cascade)

  @@index([centerId, periodType, periodStart])
  @@map("center_report_snapshots")
}

model ParentReportSnapshot {
  id          String           @id @default(cuid())
  studentId   String
  parentId    String
  periodType  ReportPeriodType
  periodStart DateTime
  periodEnd   DateTime

  // Student Information
  studentName          String
  studentGrade         String
  studentAge           Int
  enrollmentDate       DateTime
  assignedEducatorName String?

  // Assessment Summary
  totalAssessments      Int
  latestAssessmentDate  DateTime?
  latestAssessmentScore Float?
  riskLevel             String? // "High", "Moderate", "On Track"
  assessmentProgress    String? // Brief summary

  // Progress Tracking
  readingProgress       Float? // Percentage improvement
  writingProgress       Float?
  mathProgress          Float?
  attentionProgress     Float?
  overallGoalCompletion Float? // Percentage of goals completed

  // Attendance & Session Participation
  totalSessionsScheduled Int
  sessionsAttended       Int
  participationRate      Float? // Percentage
  lastSessionDate        DateTime?

  // Individual Intervention Plan - Focus Areas
  focusReading     Boolean @default(false)
  focusWriting     Boolean @default(false)
  focusMathematics Boolean @default(false)
  focusAttention   Boolean @default(false)
  focusConfidence  Boolean @default(false)

  // Intervention Goals
  shortTermGoals String? // JSON array or text
  longTermGoals  String? // JSON array or text

  // Intervention Strategies (per area)
  readingStrategy     String?
  writingStrategy     String?
  mathematicsStrategy String?
  attentionStrategy   String?
  confidenceStrategy  String?

  // Additional Information
  educatorNotes         String?
  parentFriendlySummary String? // AI-generated or educator-written
  nextReviewDate        DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  student Student       @relation(fields: [studentId], references: [id], onDelete: Cascade)
  parent  ParentProfile @relation(fields: [parentId], references: [id], onDelete: Cascade)

  @@index([studentId, periodType, periodStart])
  @@index([parentId, periodStart])
  @@map("parent_report_snapshots")
}
